{
  "name": "@finora/platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --maxWorkers=2 --passWithNoTests"
  },
  "dependencies": {
    "decimal.js": "10.6.0",
    "zod": "4.1.5"
  },
  "devDependencies": {
    "vitest": "3.2.4"
  }
}
